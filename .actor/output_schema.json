{
  "actorOutputSchemaVersion": 1,
  "title": "India E-commerce Price Tracker Output",
  "description": "Structured product candidates with explainable match confidence plus a compact cross-store comparison report.",
  "type": "object",
  "properties": {
    "results": {
      "type": "string",
      "title": "E-commerce price records",
      "description": "Clean dataset items containing export-ready product price, discount, rating, stock, URL, and image data across supported Indian marketplaces.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "matchReport": {
      "type": "string",
      "title": "Product match report",
      "description": "Markdown report showing the best candidate per source, confidence score, evidence, stock, price, source link, and observed price spread for each target product.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/MATCH_REPORT"
    }
  }
}
